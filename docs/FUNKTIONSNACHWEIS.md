# Funktionsmatrix und Freigabenachweis

Diese Matrix verbindet die historischen eStab-Funktionen mit einem
reproduzierbaren Nachweis. Sie verhindert, dass eine grüne Readiness-Antwort
mit einer vollständigen fachlichen Freigabe verwechselt wird.

## Automatisierte Nachweise

| Funktion | Automatisierter Nachweis | Freigabekriterium |
| --- | --- | --- |
| SVN-/Release-Herkunft | `migration/verify_svn_refs.py`, `migration/verify_provenance.py --self-test`, `tests/php/provenance_security.php`, `migration/verify_release_snapshot.py`, deterministische Manifeste und dokumentierte SHA-256-Werte | 13 Git-Ref-Snapshots sind gebunden: Trunk, vier Branches und sechs SVN-Tags stimmen mit den zuvor lokal verglichenen SVN-r85-Pfaden überein; hinzu kommen die SourceForge-Release-Tags 0.9.26b/c. Der eine separate Dokument-r85-Baum enthält exakt 95 unveränderte Dateien, ohne eine Dokument-Einzelhistorie zu erfinden. Refobjekt, Commit, Tree, Modus, Größe, Inhalt und rohe Unicode-Pfade sind gebunden; bei 0.9.26b/c muss die in `sourceforge-releases.tsv` aufgezeichnete Originalarchiv-SHA-256 exakt mit annotiertem Tag und Snapshot-Commit übereinstimmen. Neu versiegelte SVN- und Release-Ref-Inhaltsmanipulation sowie Manifest-, aufgezeichnete Releaseidentitäts- und Dokumentmanipulation werden negativ erkannt. CI lädt keine externen Archive neu; sie beweist Taginhalt und gebundene aufgezeichnete Archividentität, während `verify_release_snapshot.py` den entpackten Payload eines separat bereitgestellten und per aufgezeichneter SHA-256 geprüften Archivs erneut dateiweise vergleichen kann. |
| GitHub-Actions-Workflows | offizielles Actionlint 1.7.12 aus dem festgelegten Multi-Arch-Index-Digest, `.github/workflows/{ci,publish-images}.yml`, `tests/php/registry_deployment_contract.php` | beide Workflows bestehen YAML-, Schema-, Ausdrucks-, Matrix-, Abhängigkeits-, Aktionsparameter-, Shellcheck- und Pyflakes-Prüfung; das Repository erzwingt den schreibgeschützten Checkout-Mount und denselben Digest im CI-Vertrag, damit insbesondere ungültige Ausdruckskontexte wie ein `runner.temp` außerhalb eines Steps nicht erneut unbemerkt bleiben |
| PHP-8.5-Laufzeit | `tests/static/run.sh` | alle aktiven PHP-Dateien linten; Kompatibilitäts- und Sicherheitsassertionen sind grün |
| MariaDB-Basisschema und konsistente Readiness | `migrate`, `docker/db/verify.sql`, `/health.php`, `/4fadm/system_status.php`, `tests/integration/http_smoke.sh`, `tests/integration/assignment_policy.php` | Migrator Exit 0; sämtliche 32 Ergebnisfelder der read-only Schema-Verifikation sind `1`; Health und Adminstatus verwenden denselben Prüfdienst; neben dem Legacy-Schema werden Nachrichtennachweis, ETB-/Retention-Grenzen, Dienstbetrieb, S6-Planung, Melderlauf, Anhang-Eingangsintegrität und beide Hashketten geprüft; eine kontrolliert fehlende Standardmatrixtabelle oder ein aktiv markiertes Konto mit ungültigem Funktions-/Rollenpaar ergibt HTTP 503 beziehungsweise „Prüfung erforderlich“, während ein inaktives Waisenkonto zur administrativen Reparatur betriebsbereit bleibt |
| Fresh-Baseline und Upgrade eines Legacy-Schemas | `tests/integration/schema_migrator.sh` | ein mit passendem Checksum protokollierter Abbruch nach der ersten Basistabelle wird idempotent bis zu allen 14 Basistabellen und nach dreizehn Migrationen 30 Runtime-Tabellen fortgesetzt; Migration 40 nimmt ausschließlich ihre markierte leere oder exakt kanonisch gesetzte eigene Tabelle wieder auf, während veränderte eigene Inhalte und eine fremde Kollision mit `nv_empfmtx_standard` unverändert blockiert bleiben; ein unprotokollierter `nv_*`-Teilbestand sowie Duplikate blockieren das Legacy-Upgrade; Migration 45 schützt den unveränderten Einsatz-Backfill 50 vor unvollständigen Tabellen und automatischen Zeitstempeländerungen, Migration 55 stellt die kanonischen Attribute wieder her, Migration 70 ergänzt die kollisionsgeprüfte Kontosperre, Migrationen 80/94 ergänzen Nachweis-/Retention- sowie Führungsstellenstrukturen, Migration 95 führt den nicht rückwirkenden Anhang-Eingangsnachweis ein und Migration 96 ergänzt die getrennte ETB-Funktion samt `EINSATZTAGEBUCH`-Fähigkeit, ohne historische Einsatzdaten umzuschreiben; Migration 96 nimmt die nach Indexentfernung, ENUM-Erweiterung oder vollständig geschriebenem Katalog unterbrochenen Eigenzustände wieder auf, blockiert dagegen gemischte Kataloge und einen abweichenden Primärschlüssel unverändert; Migration 97 ergänzt die exakt markierte nullable Führungsstellenspalte, den dauerhaften Sperrmarker und die DB-Schreibgrenze, lässt historische Namen `NULL` und blockiert fremde Spalten-, Routine- und Triggerkollisionen; Migration 98 ergänzt Rufnummer und Betreff an der kanonischen Position, bewahrt Bestandswerte und blockiert fremde Spaltenzustände; der Standardmatrix-Seed stimmt zellgenau mit der 20-Zellen-Fixture überein; Zweitlauf, Engine, Zeichensatz und Inhalte dynamischer Tabellen bleiben korrekt; Checksum-Manipulation blockiert |
| Datumsmigration | `tests/integration/date_compatibility.php` | Zero Dates werden `NULL`; gültige Werte und SQL-Mode bleiben erhalten |
| dynamische Benutzer-/Funktionstabellen | `tests/integration/dynamic_tables.php`, `tests/integration/dv_operations.php` | der gemeinsame, advisory-lock-geschützte Schema-Reconciler hält die sechs Legacy-Tabellen samt Daten, Duplikaten, Engine, Collation und Indizes korrekt; die Auswahl einer zusätzlich angenommenen Stabs-/FB-Funktion stellt deren Tabellen vor dem Sitzungswechsel und vor der Domänentransaktion bereit, während die reine ETB-Funktion absichtlich keine Nachrichten-/Kategorietabellen erhält |
| Globaler Einsatz und Eingabesperre | `tests/php/incident_domain_security.php`, `tests/php/incident_ui_security.php`, `tests/php/operational_incident_scope.php`, `tests/integration/incident_domain.php`, `tests/integration/incident_ci_bootstrap.php` | genau ein revisionsgesicherter aktiver Einsatz; ohne aktiven Einsatz oder bei historisch fehlendem Führungsstellennamen blockieren operative Eingaben, während Anmeldung und Administration möglich bleiben; neue Einsätze verlangen einen getrennten Führungsstellennamen, die einmalige Bestätigung eines migrierten `NULL`-Werts bleibt trotz Altdaten möglich und die erste Fachänderung setzt atomar einen dauerhaften Sperrmarker; direkter Legacy-SQL, Direktänderungen sowie Insert→Delete→Umbenennen bleiben fail-closed; Aktivierung kann keinen laufenden Writer überholen; Insert, Update, Delete, Listen, Zähler, Nebenstatus, ETB/TBB und Audit bleiben in ihrem Einsatz; Bestandsdaten landen ausschließlich in `LEGACY-IMPORT` |
| Benutzerverwaltung | `tests/php/user_admin_security.php`, `tests/integration/user_admin.php` | Kontoanlage speichert nur einen Hash und eine serverseitig abgeleitete feste Funktion/Rolle; leere Legacy-Zuordnungen bleiben reparierbar; Neuzuweisung, Sperren, Entsperren und Kennwortreset teilen den Login-Lock, widerrufen alte Sitzungen, rollen bei Auditfehler vollständig zurück und protokollieren niemals Kennwort, Hash oder SID; alter Login scheitert nach Reset beziehungsweise Neuzuweisung, nur neues Kennwort und neue Funktion funktionieren |
| Anmeldung, Navigation, Sitzungsanzeige und Rollenbindung | `tests/php/auth_security.php`, `tests/php/navigation_security.php`, `tests/php/read_authorization_security.php`, `tests/php/session_ui_security.php`, `tests/php/root_menu_security.php`, `tests/php/workflow_security.php`, `tests/browser/headless_ui.py`, `tests/integration/user_admin.php`, `tests/integration/http_surface_http.sh`, `tests/integration/http_smoke.sh`, `tests/integration/legacy_login_http.sh`, `tests/integration/categories_http.sh`, `tests/integration/logbooks_http.sh` | Der voranmelde-CSRF-gebundene Bestandskonto-Flow und die standardmäßig deaktivierte öffentliche Registrierung funktionieren. Tests melden administrativ provisionierte Konten an; aktive wie inaktive Konten können Funktion/Rolle nicht per Request wechseln. Reale Bestandslogin- und Self-Registration-Audits enthalten die exakte SHA-256-Referenz der rotierten Sitzung, aber weder rohe SID noch Kennwort. Der vollständige HTTP-Lauf weist tokenlose Anmeldung im Default `false` auch bei Same-Origin mit HTTP 403 ab; nur ein separater kurzlebiger Opt-in-Lauf belegt den historischen Ein-Kennwort-Client, weiterhin geschlossene Cross-Site-Anfragen und den erreichbaren Führungsstellen-Bootstrap. Auch dieser Login verleiht keinen Funktionshut: Privilegierte Vordrucke bleiben bis zur persönlichen Auswahl mit HTTP 403 geschlossen. Anonyme direkte Fachseiten und abgelaufene Sitzungen zeigen keine blockierende Textseite: navigierbare GET-Aufrufe und sichere POST-Unterbrechungen antworten mit HTTP 303 zum ausdrücklichen Bestandslogin, übernehmen ausschließlich einen symbolischen Allowlist-`next`-Schlüssel und bieten einen sichtbaren Abbruch zurück zur öffentlichen Übersicht. Frame-lokale Anhangs-, Kategorie- und Nachrichtenflüsse verwenden das Content-Login und erzeugen keinen verschachtelten Arbeitsbereich. Gemeinsame Navigation, Zielbeibehaltung, Sessionanzeige, Sidebar-/Browserlayouts, Logout-/SID-Widerruf, Dirty-Guard und responsive Bedienziele sind automatisiert belegt. Das Manifest enthält neun Bereiche und zwei Dienste: anonym elf Links, vor Hutauswahl ausschließlich die vier öffentlichen/separat geschützten Ziele plus Führungsstellen-Bootstrap, nach Auswahl abhängig vom Funktions-Hut neun beziehungsweise zehn Links; unzulässige Spezialziele fehlen. |
| Reverse-Proxy-Vertrauen | `tests/php/runtime_compatibility.php`, `tests/php/auth_security.php`, `tests/php/registry_deployment_contract.php` | Forwarded-Header sind standardmäßig wirkungslos; aktiviertes Vertrauen verlangt eine nichtleere, in beiden Compose-Pfaden ausgelieferte IPv4-/IPv6-IP-/CIDR-Allowlist, lehnt Hostnamen, ungültige Präfixe und Catch-all-Netze ab und gilt pro Request nur bei passender direkter `REMOTE_ADDR`; erst innerhalb dieser Grenze werden vollständig valide Proto-/Adressketten für Secure-Cookie und Audit ausgewertet |
| Rollenabhängige Warteschlangenhinweise | `tests/php/sidebar_ui_security.php`, `tests/php/session_ui_security.php`, `tests/php/audio_asset_security.php`, `tests/browser/headless_ui.py` | Die vollständige Fernmelder-/Si-/Stab-/FB-Profilmatrix, vorbereitete Matrix-/Queue-Abfragen, serverseitige `partial`-/`unavailable`-Zustände, begrenzter Poll-Timeout und ausschließlich gebündelte gleich-originige PCM-WAV-Dateien sind statisch belegt; die drei fest gebundenen Töne werden zusätzlich vollständig als RIFF/WAVE PCM-16 geparst und gegen SHA-256, Kanäle, Abtastrate, Framezahl, Dauer, Signalspitze und Mindest-RMS geprüft, sodass beschädigte, ausgetauschte oder stumme Assets die Freigabe sperren; Datenbankfehler erreichen nicht mehr die beendenden Legacy-Includes/-Zähler; die erste erfolgreiche Messung initialisiert genau den passenden `old_que_*`-Basiswert ohne Hinweis, spätere erfolgreiche Messungen schreiben ihn fort und nur eine Erhöhung markiert genau einmal eine Wiedergabe, während Gleichstand, Rückgang oder fehlender Messwert keine falsche Auslösung erzeugen; ein positiver Zähler bleibt dauerhaft hervorgehoben; der echte authentifizierte Browser weist bei 390 × 844 CSS-Pixeln sichtbaren 503-/Erholungspfad, Opt-in, `NotAllowedError`, ehrlichen Reload-Zustand, `StorageEvent`-Synchronisation, Race-Abbruch, automatischen Auslösemarker, Fokus-/Live-Knoten-Erhalt und dasselbe langlebige Audioelement nach; physische Hörbarkeit bleibt manuelles Freigabekriterium |
| Stab-Nachricht und Vorrang | `tests/php/message_security.php`, `tests/php/message_priority_security.php`, `tests/php/read_authorization_security.php`, `tests/integration/http_smoke.sh` | Formular öffnet nur mit ausgewählter, persönlich angenommener aktiver Dienstfunktion; rohe UTF-8-Nachricht wird gespeichert und wieder gelesen; Quotes, `&`, `<script>` und SQL-ähnlicher Text bleiben Daten; GET-Detail, fremde Objekte, Kontositzungen ohne ausgewählten Hut und ungültige IDs werden abgewiesen. Vorrang wird zentral als keine, Sofort, Blitz oder Staatsnot dargestellt, streng gegen Einzelwerte validiert und mit einer expliziten fachlichen Rangfolge sortiert; das historische `eee` bleibt als „keine“ lesbar, wird bei neuen Nachrichten aber nicht angeboten. Staatsnot trägt einen organisatorischen Weisungshinweis, ohne eine externe Anordnungsbefugnis aus einer App-Rolle abzuleiten. |
| Rollenübergreifender Nachrichtenlauf | `tests/php/config_security.php`, `tests/php/official_message_form_ui_security.php`, `tests/php/message_form_fields_security.php`, `tests/php/ldf_validation_security.php`, `tests/php/ldf_ui_flow_security.php`, `tests/php/read_authorization_security.php`, `tests/php/message_suggestion_security.php`, `tests/integration/message_suggestions.php`, `tests/integration/message_workflow_http.sh`, `tests/browser/headless_ui.py`, orchestriert durch `tests/integration/ci.sh` | isolierte Konten für LdF, A/W, Si, S1, S2, S3 und POL/FB durchlaufen genau einen verbindlichen Ablauf: Eingang `A/W → LdF → Si → abgeschlossen`, Ausgang `Verfasser → Si → LdF → A/W → abgeschlossen`; eine formale Rückgabe geht mit Pflichtgrund an den Verfasser und muss nach Korrektur erneut durch Si. Der Bildschirmvordruck bewahrt das amtliche Dreizonenraster und den Blauton `#A2D9F7`, enthält genau 20 zugängliche Ausfüllhilfen und bleibt mobil innerhalb des Blatts horizontal scrollbar, ohne die Zellfolge umzubauen. Rufnummer und Pflichtbetreff werden getrennt validiert und persistiert; die Antwort übernimmt die Rufnummer und erhält `AW:`, die Weiterleitung beginnt ohne Rufnummer und erhält `WG:` – jeweils ausschließlich aus dem serverautorisierten Ausgangsdatensatz abgeleitet. Der ältere zusätzliche Beförderungshinweis bleibt außerhalb des amtlichen Rasters erhalten. LdF übersetzt beim Eingang den erfassten Rufnamen in den Absender und bestätigt den von A/W erfassten Eingangsweg; eine Änderung verlangt eine Begründung und schreibt ursprüngliches sowie bestätigtes Medium mit LdF-Identität atomar in die Ereigniskette, während A/W-Aufnahmezeit und -zeichen unverändert bleiben. Beim Ausgang entscheidet LdF Gegenstellenrufname und vorgesehenen Beförderungsweg; A/W dokumentiert erst danach tatsächlichen Weg und Beförderungszeit. Die Fokus-Listbox zeigt A/W und LdF nur Rufnamen aus dem aktuell aktiven Einsatz sowie ausschließlich LdF bei Eingängen bisherige Absender. Für LdF korreliert sie den im gesperrten aktuellen Eingang aufgenommenen Rufnamen mit abgeschlossenen Absenderpaaren beziehungsweise die Ausgangsanschrift mit abgeschlossenen Gegenstellenrufnamen: häufige und aktuelle Paare stehen vor einem passenden gültigen aktiven S6-Fernmeldeplan und der allgemeinen Einsatzhistorie. Herkunft und Kontext sind sichtbar; exakte Dienstbesetzung, Rolle, Fähigkeit, Richtung, Status, Sperrbesitz und Einsatzgrenze werden im selben SQL-Statement erneut geprüft. Akzentverschiedene Werte bleiben getrennt und andere Einsätze ausgeschlossen. Die per Tastatur bedienbare Liste wählt nichts automatisch aus, freie Eingaben bleiben möglich, die Browser-Autovervollständigung ist aus, ein natives `datalist` bleibt ohne JavaScript verfügbar. Lokale Eingangsanschrift und Ausgangs-Absendereinheit stammen ausschließlich aus dem Führungsstellennamen des in derselben Schreibtransaktion gesperrten Einsatzes; Einsatzname, Organisation, Browser- und Umgebungswerte sind keine Quelle. Ein echtes A/W-Konto beweist in Chrome Fokusöffnung, Filterung, Pfeiltaste/Eingabetaste, Übernahme und freie Eingabe gegen einen kurzlebigen einsatzgebundenen Marker. Bedieneridentität, Funktion und die anfängliche S2-Rotkopie stammen aus der servergeprüften Sitzung beziehungsweise Konfiguration; A/W kann weder Absender noch Empfänger einschleusen. Ohne LdF-Bestätigung oder bei unbegründeter Wegkorrektur bleiben Nachricht und Ereigniskette unverändert. Fremde Empfänger erhalten Ein- und Ausgänge erst nach Status `8`, während die Verfasserfunktion ihren eigenen Ausgang durch Sichtung, Rückgabe und Transport verfolgen darf; Si, LdF und A/W behalten nur Zugriff auf die eigene aktuelle Warteschlange/Sperre oder die eigene unveränderliche Verarbeitungsmarke. Liste, Detailzugriff, Vordruck und Gelesen-/Erledigt-Zustand verwenden dieselbe Objektregel. Sperrbesitz und Statusfolge sind konkurrenzsicher, Autosichtung und konfigurierbare Abkürzungen sind ausgeschlossen. |
| Nachrichtenstatus, Ereigniskette und Parallelität | `tests/php/dv_evidence_security.php`, `tests/integration/message_concurrency.php`, `tests/integration/categories_http.sh` | parallele Nummern kollidieren nicht; fremde Sperrinhaber verlieren; beim Save-/Reset-Rennen gewinnt genau eine Änderung. Jede kanonische Nachrichtenerzeugung und jeder Zuständigkeitswechsel schreibt im selben Commit ein strukturiertes Ereignis. MariaDB verhindert Änderung und Löschung, prüft Snapshot- und Verkettungshash und hält einen serialisierten Kopf je Nachricht; der Verifikator rekonstruiert die vollständige Kette. Read-/Done-Zustände bleiben getrennt idempotent. |
| Dienstschicht und Mehrfachfunktion | `tests/php/dv_operations_security.php`, `tests/integration/dv_operations.php`, `tests/integration/http_smoke.sh` | Schichten werden einsatzgebunden geplant, mit vollständig angenommenen Pflichtfunktionen aktiviert und zweistufig übergeben: Administration initiiert, ein persönlich angemeldetes Konto der Nachfolgeschicht bestätigt. Vor Hutauswahl lässt der Führungsstellen-Bootstrap nur Einsatz-/Schichtgrunddaten, eigene Besetzungen, persönliche Annahme/Übergabebestätigung und die Auswahl einer eigenen aktiven, angenommenen Besetzungs-ID zu; Telekommunikationspläne, Melderaufträge und sonstige operative Daten bleiben gesperrt. Jeder normale Schreibpfad validiert die exakt in der Sitzung ausgewählte ID erneut gegen Konto, Funktion, Rolle, aktiven Einsatz und aktive Schicht; fremde, abgelaufene oder nur funktionsgleiche IDs scheitern. Offene Fehlanforderungen werden nur begründet und revisionssicher storniert. Ungesperrte Offline-Konten dürfen vorausgeplant werden; eine in der Websitzung persönlich gespeicherte Annahme zählt auch nach Logout, weil `aktiv` nur Onlinezustand ist. Gesperrte Konten zählen nie. Ein echter Sitzungsnachweis wechselt dasselbe Konto von S2 nach S3 und ein zweites von Si nach ETB; er schreibt unter S3 einen Gesprächsvermerk, Gelesen-/Erledigt-Zustände sowie Funktions-/Benutzerkategorien und beweist beim ETB-Hut die getrennte, eng begrenzte Fachzuständigkeit. Abgelöste Funktionen referenzieren ihre angenommene Nachfolgebesetzung derselben Funktion/Rolle auch bei Personenwechsel. Die letzte aktive Schicht schließt erst bei sauberem fachlichem Abschluss-Preflight; eine offene Status-4-Nachricht hält sie nachweislich offen. Der echte Admin-POST erhält den Anhang-Ablageroot: Ein direkt davor gleichlang manipulierter Pflichtanhang liefert HTTP 409 mit „Anhang-Integritätsfehler“, während Schichtstatus/-endzeit und alle Besetzungsstatus/-ablösezeiten exakt unverändert bleiben. |
| S6-Fernmeldeplan und Melderlauf | `tests/php/dv_operations_security.php`, `tests/integration/dv_operations.php` | nur die aktuell ausgewählte aktive S6-Besetzung erstellt und veröffentlicht einsatzgebundene Planversionen mit Betriebsstellen, Rufnamen, Medium, Kanal/Rufgruppe, Bandlage, Verkehrsform und Betriebsleitung; Planidentität und veröffentlichte Fassungen sind unveränderbar. Ein von der ausgewählten LdF-Besetzung beauftragter Melder bestätigt persönlich Übernahme, Übergabe mit Empfänger, Rückweg und Rückkehr; nur LdF bestätigt danach die Rückmeldung an die FmZt. Ziel und bereits gesetzte Evidenzfelder bleiben bei jedem Folgeschritt unverändert. Jeder Übergang wird hashverkettet nachgewiesen. |
| Anhang | `tests/php/upload_security.php`, `tests/php/attachment_security.php`, `tests/php/file_access_security.php`, `tests/php/read_authorization_security.php`, `tests/integration/attachment_reservation.php`, `tests/integration/http_smoke.sh`, `tests/browser/headless_ui.py` | parallele Reservierungen kollidieren nicht; ein nach erfolgreichem `execute()` erst beim Puffern gemeldeter MariaDB-Timeout 1205 behält seinen Fehlercode; ein echter, am InnoDB-Zähler und am betroffenen Worker beobachteter Deadlock 1213 durchläuft den begrenzten Produktions-Rollback/-Retrypfad und beide Worker schließen erfolgreich ab; Claim, Dateiablage, Finalisierung und Audit halten denselben Einsatz-Lock, Fehler geben die Reservierung frei und entfernen eine bereits bewegte Datei; Finalisierung bindet SHA-256, Bytezahl und Serverzeit atomar und Datenbank-Trigger machen den Nachweis unveränderlich; ein verknüpfter Anhang erbt über vollständige Dateinamens-Tokens exakt die Leserechte mindestens einer verknüpften Nachricht, ein freier Anhang ist nur für Uploader oder ausgewähltes S2, Si beziehungsweise LdF sichtbar; Liste, Download, Vorschau, Auswahl und finaler Nachrichtensave wiederholen dieselbe selected-hat-/Objektprüfung; Download und Bildvorschau prüfen zusätzlich einen privaten Snapshot und verarbeiten genau dessen bereits gehashtes Handle, während eine gleich große Byte-Manipulation vor Binär- beziehungsweise Bild-Headern mit HTTP 409 scheitert; ein echtes JPEG über der früheren 5-MiB-Grenze wird mit `.JPEG` akzeptiert, als `.jpeg` normalisiert und durch Datenbankmetadaten, bytegleichen Download, echten MIME-Typ sowie dekodierte Vorschau nachgewiesen; eine nur umbenannte Fremddatei und ein JPEG über 20 MiB werden mit verständlichem Grund abgewiesen, ohne Datei, finale Metadaten oder aktive Reservierung zurückzulassen; der Browser prüft beschrifteten Dateidialog, Formatliste, Größenlimit und sauberen Abbruch |
| Nachweisung und Meldungsübersicht | `tests/php/message_transport_security.php`, `tests/php/read_authorization_security.php`, `tests/integration/http_smoke.sh`, `tests/integration/message_workflow_http.sh` | Beide Ansichten sind einsatzgebunden und verlangen einen ausgewählten aktiven Funktions-Hut: Meldungsübersicht ausschließlich S2/`LAGE_DOKUMENTATION`, Nachweisung ausschließlich LdF/`FERNMELDEBETRIEB` oder A/W/`BEFOERDERUNG`. LdF-, A/W-, Si-Warteschlangen, getrennte und gemeinsame Nachweisung sowie Meldungsübersicht reichen die beim Gate erfasste Einsatz-ID bis in jede vorbereitete Liste und Detailabfrage; ein paralleler Einsatzwechsel kann keine Fremddaten unter der alten Autorisierung mischen. Die Nachweisung übersetzt Eingangsmedien lesbar und zeigt bei Ausgängen den kombinierten Medium-/Freitextweg erst nach gesetzter Beförderungszeit als tatsächlich befördert, vorher ausdrücklich „Noch nicht befördert“; unbekannte Legacywerte bleiben sichtbar und HTML-inert. |
| Nachrichtenvordruck-PDF | `tests/php/pdf_smoke.php`, `tests/php/generated_form_security.php`, `tests/php/read_authorization_security.php`, `tests/php/pdf_template_render_fixture.php`, `tests/static/pdf_render.sh`, `tests/integration/http_smoke.sh` | die echte historische Vordruckklasse erzeugt ein vollständiges durchsuchbares A4-PDF ohne VS-NfD-Aufdruck, Wappen oder Seitenbild; Rufnummer und Betreff erscheinen in der amtlichen Reihenfolge vor dem Nachrichtentext; die Vorrangsstufe erscheint als verständliches Sofort, Blitz oder Staatsnot und bleibt bei keiner beziehungsweise historischem `eee` leer, niemals als Rohkürzel; Einzelvordruck und Dossier-Nachrichtenseite werden aus identischen Daten ein- und mehrseitig pixelgleich gerendert, ihr Folgeseiteneinzug bleibt identisch und nicht mehr in der Matrix vorhandene Empfänger bleiben samt Kopiekennzeichen sichtbar; ein abgeschlossener Workflow stößt den realen Generator an und veröffentlicht atomar `<datenbank> Einsatz-<einsatz_id> <nummer> <E\|A>.pdf`; Liste, aktueller In-Memory-Abzug und unveränderter Archivdownload verlangen erneut aktiven Einsatz, ausgewählte aktive Dienstbesetzung und Leserecht an der zugrunde liegenden Nachricht; der aktuelle Abzug nutzt denselben Renderer und dieselbe vollständige Matrix, während ein absichtlich veraltetes gültiges Archiv separat bytegleich heruntergeladen und mit seinem SHA-256-Wert an den Restore-Nachweis gebunden bleibt; leere, unbekannte oder nichtskalare Layoutwerte werden abgewiesen |
| PDF-Einsatzdossier | `tests/php/incident_pdf_security.php`, `tests/php/incident_export_security.php`, `tests/php/pdf_template_render_fixture.php`, `tests/static/pdf_render.sh`, `tests/integration/incident_export.php` | Die neun wählbaren Abschnitte ETB, TBB, Nachrichtenvordrucke, Anhänge, Nachrichtenereignisse, Dienstbetrieb, S6-Fernmeldepläne, Melderläufe und Betriebsereignisse werden aus einem konsistenten Read-only-Snapshot des ausdrücklich gewählten aktiven oder historischen Einsatzes gelesen; Führungsstellenname, Einsatzkennung und Einsatzname erscheinen getrennt, ein historischer `NULL`-Wert ausdrücklich als „historisch nicht erfasst“; jeder neue Anhang muss vor dem Laden und nochmals beim Einbetten gegen seinen unveränderlichen SHA-256-/Größennachweis bestehen, während Legacy-Dateien ausdrücklich „Integrität beim Eingang nicht belegbar“ heißen; jede Nachricht verwendet den gemeinsamen Einzelvordruck-Renderer und die vollständige Empfängermatrix, historische Anhangnamen erhalten keinen Link zum aktiven Einsatz; ein zweiter Einsatz bleibt vollständig ausgeschlossen; Poppler belegt A4, durchsuchbaren Text, fehlende Bilder, alle neun Abschnitte, pixelgleiche Vorlagen und die bytegleiche Anhangextraktion, der echte MariaDB-Test zusätzlich die Ablehnung gleich großer manipulierter Bytes beim Laden, Einbetten, Abschluss-Preflight und tatsächlichen formalen Abschluss sowie Datei- und Gesamt-PDF-SHA-256 |
| ETB/TBB | `tests/php/logbook_security.php`, `tests/php/read_authorization_security.php`, `tests/integration/logbooks_http.sh`, `tests/integration/dv_operations.php`, Restore-Zweig in `tests/integration/http_smoke.sh` | jede ausgewählte, persönlich angenommene aktive Dienstfunktion darf beide Bücher des aktiven Einsatzes lesen; eine bloße Kontoanmeldung bleibt ausgeschlossen. ETB schreibt ausschließlich eine ausgewählte S2- oder ETB-Besetzung mit `EINSATZTAGEBUCH`, TBB ausschließlich A/W mit `BEFOERDERUNG`; insbesondere erhält der mit ETB kombinierbare Si-Hut kein ETB-Schreibrecht und der ETB-Hut weder `LAGE_DOKUMENTATION` noch Meldungsübersicht, Rotkopien oder Nachrichten-/Kategorietabellen. Cross-Rollen-Schreibversuche liefern HTTP 403; beide Bücher zeigen statt eigener Titel den globalen Einsatzkopf mit Führungsstellenname; nach dem Restore werden Einsatzkopf und zuvor geschriebene Einträge ausschließlich lesend wiedergefunden und nicht durch den Prüflauf neu angelegt |
| Kategorien | `tests/php/category_security.php`, `tests/integration/categories_http.sh` | anonyme Zugriffe, GET-Mutationen, fehlendes CSRF, fremde Tabellenräume und fremde Meldungs-IDs werden abgewiesen; S1 verwaltet nur eigene Funktions-/Benutzerkategorien, S2/Rotkopie und Si verwalten Master; CRUD und Zuordnung sind atomar; Quotes, `&` und `<script>` bleiben roh in MariaDB und werden in HTML neutralisiert |
| Empfängermatrix und Standard | `tests/php/assignment_policy_security.php`, `tests/php/admin_operations_security.php`, `tests/integration/assignment_policy.php`, `tests/integration/admin_workflows_http.sh`, `tests/browser/headless_ui.py` | aktive Matrix und persistente Standardmatrix enthalten jeweils exakt 20 eindeutige Positionen; S2/Stab ist in beiden die unveränderliche Lage-/Dokumentationsfähigkeit und der einzige Rotkopie-Empfänger, alle Autosichtungswerte bleiben falsch. Laden und Speichern sind getrennt, atomar und CSRF-geschützt; ein globaler Richtlinienlock serialisiert Login, Kontoanlage, Neuzuweisung und Matrixspeichern. Rollenänderungen synchronisieren Konten und widerrufen Sitzungen, entfernte Funktionen bleiben administrativ reparierbar; ein erzwungener Auditfehler lässt Matrix, Konten, Sitzungen und Audit unverändert. |
| Nachrichtenzähler und PDF-Vordruckreset | `tests/php/admin_operations_security.php`, `tests/integration/message_concurrency.php`, `tests/integration/admin_workflows_http.sh` | GET und fehlendes CSRF sind inert; Admin-Reparatur und regulärer Writer teilen den aktiven Einsatz-/Zähler-Lock; zwei konkurrierende Erhöhungen erzeugen genau ein hashverkettetes `message_counter_repaired`-Betriebsereignis und keine Status-0-Fachnachricht; die nächste echte Nachricht folgt auf den reparierten Papierwert, Nachrichten- und Betriebskette bleiben gültig; Vordruckflags werden nur für den aktiven Einsatz und erst nach bestätigtem POST zurückgesetzt; Audit ist einsatzgebunden und vollständig |
| Administration und Export | `tests/php/export_security.php`, `tests/integration/http_smoke.sh`, `tests/browser/headless_ui.py` | anonymer Zugriff wird abgewiesen; Basic Auth und CSRF schützen die passenden Aktionen; vor einem Tabellenexport werden alle neuen Anhangdateien gegen ihren Eingangsnachweis geprüft, das validierte Manifest zählt verifizierte und ausdrücklich nicht belegbare Legacy-Dateien; die Einsatz-CSV enthält den getrennten Führungsstellennamen und bewahrt historische Fehlwerte als `NULL`; reguläre ZIPs werden neueste zuerst gelistet und unverändert heruntergeladen; Traversal/Symlinks bleiben außerhalb; eine zweistufig bestätigte Einzellöschung entfernt nur das gewählte Verzeichnis-/ZIP-Paar und lässt einen zweiten Export unverändert |
| Backup/Wiederherstellung | CI-Roundtrips und `docs/BACKUP-UND-WIEDERHERSTELLUNG.md` | der fachliche guardierte Lauf löscht Container und alle drei benannten CI-Volumes, stellt sie leer neu bereit und weist danach Schema, Anmeldung/Konto, Nachricht, exakten Anhanginhalt, bytegleichen persistierten PDF-Vordruck, globalen Einsatzkopf samt Führungsstellenname, vorhandene ETB-/TBB-Einträge sowie genau den zuvor validierten Exportlauf mit unverändertem ZIP-SHA-256 nach; ein historischer `NULL`-Wert wird nicht erfunden; ein zweiter Pull-only-Roundtrip verfälscht den Datenbankmarker logisch, ergänzt veraltete Testdaten in `4fdata` und Export und stellt alle Marker mit unverändertem Inhalt und SHA-256 wieder her, ohne den rohen DB-Bind-Mount zu leeren; die Archive gelangen bei der Wiederherstellung ausdrücklich über interaktive Standardeingabe in die netzlosen Hilfscontainer |
| Pull-only OCI-/NAS-Deployment | `tests/php/registry_deployment_contract.php`, `tests/integration/registry_compose.sh`, `.github/workflows/{ci,publish-images}.yml` | Registry-Compose enthält keinen Build und keinen Host-Schema-Mount; der gebundene Release-Verifier und `deploy.sh` verlangen explizite unveränderliche Image-Referenzen und akzeptieren kein stilles `latest`; vor Pull und erneut unter der engine-weiten Wartungssperre vor Start werden dedizierte Named Volumes sowie bestehende, nicht symbolische, ACL-geprüfte und paarweise getrennte Bind-Verzeichnisse erzwungen, während Root-/Top-Level-/Release-Ahnenpfade und eine Aliasierung auf `estab_auth` scheitern; nur der netzlose `admin-auth-init` sieht das Klartextsecret, die App erhält ausschließlich den bcrypt-Hash mit Kostenfaktor 12 schreibgeschützt; ein frisches Zweitprojekt startet ausschließlich die angeforderten Image-IDs und ein weiteres Projekt verwendet nachweislich echte Bind-Mounts für DB, `4fdata` und Export, initialisiert das eingebettete Basisschema, verlangt vor und nach Restore Migrator Exit 0 und eine gesunde App und beweist vor dem grünen Ergebnis die Entfernung beider Projekte samt Containern, Volumes, Netzwerken und temporärem Hostbaum; normales CI und manuell freizugebender Publish-Pfad führen das komplette Container-/Restore-Gate nativ auf amd64 und arm64 aus, der Publish-Pfad bleibt geschützt-Git-Tag-, Ruleset-, Rechte-, Variablen-, Immutable-Policy- und Required-Reviewer-gebunden, pusht App und Migrator ausschließlich digest-only, liest SPDX-SBOM sowie Build-Provenance beider Plattformen, verifiziert Image- und Release-Attestationen, blockiert behebbare hohe/kritische CVEs in App, Migrator und DB und veröffentlicht exakt vier gebundene Assets: Installations- und dauerhaftes Evidence-Archiv samt äußerer SHA-256-Datei; ein Offline-Helfer bewahrt die drei digesttreuen Multi-Arch-OCI-Indizes und prüft eine administrativ bereitgestellte Registry ausschließlich über Digestreferenzen ohne selbst Registry-Tags zu schreiben |
| Angriffsfläche des Containers | HTTP-Smoke, Admin-HTTP-Integration und Apache-Konfiguration | interne Konfiguration/Datendateien, der alte Alle-Nachrichten-View, interne Print-/FPDF-Bibliotheken und gefährliche Uploadhelfer liefern 403; nur die expliziten Direktupload-Tombstones liefern 410; Security Header sind vorhanden |

Die CI-Orchestrierung liegt in `tests/integration/ci.sh`. Sie verwendet
ephemere Secrets, eigene Volumes, harte Zeitgrenzen, Fehlerartefakte und
entfernt den Teststack am Ende. PHP-Warnungen, Notices, Deprecations, Fatals
und ungefangene Fehler im App-Log sperren die Freigabe. GitHub Actions setzt
den Browsermodus auf `required`; lokal läuft er standardmäßig automatisch,
wenn Python 3 und Chrome/Chromium verfügbar sind. Ein lokales `SKIP` ist kein
vollständiger Freigabenachweis. Browserfehler sichern Screenshot und
kennwortfreien Frame-/Session-Zustand im CI-Diagnoseartefakt. Der
Nachrichtenrollenlauf verwendet ausschließlich den verbindlichen DV-Laufweg.
Eine zweite Konfigurationsvariante, Autosichtung oder ein Bypass werden nicht
mehr getestet, weil die Anwendung diese Abkürzungen nicht mehr anbietet.

## Verpflichtende fachliche Bedienabnahme

Die folgenden Interaktionen hängen von der organisationsspezifischen
Empfängermatrix, Rollenbesetzung und fachlichen Bedeutung der Daten ab. Sie
werden vor dem ersten Produktiveinsatz und nach fachlich relevanten Änderungen
mit den tatsächlich verwendeten Funktionen im Browser abgenommen:

- einen Eingang über A/W, LdF und Si sowie einen Ausgang über Verfasser, Si,
  LdF und A/W vollständig bearbeiten; zusätzlich einen formal zurückgegebenen
  Ausgang korrigieren und erneut einreichen,
- mit getrennten ausgewählten Funktions-Hüten positive und negative
  Lesetests durchführen: S2-only Meldungsübersicht, LdF-/A/W-only
  Nachweisung, terminale Empfängerkopie, eigener Ausgang, eigene
  Si-/LdF-/A/W-Verarbeitungsmarke sowie ein jeweils fremdes Objekt,
- einen verknüpften und einen freien Anhang über Liste, Vorschau, Download,
  Auswahl und endgültiges Nachrichtenspeichern mit berechtigtem und
  unberechtigtem Funktions-Hut prüfen,
- rote/grüne Kopien sowie Priorität, Gesprächsnotiz und Sperrverhalten prüfen,
- globale, funktionsbezogene und persönliche Kategorie anlegen, zuweisen,
  suchen und wieder entfernen,
- Empfängermatrix und Rollenbezeichnungen gegen den örtlichen Führungsaufbau
  prüfen; S2-Rotkopie und ausgeschlossene Autosichtung kontrollieren und
  sicherstellen, dass bereits angemeldete Funktionen organisatorisch neu
  zugeordnet werden,
- Nachrichtenzähler nach einer simulierten Papierrückfallebene ausschließlich
  erhöhen und verketteten Zählernachweis sowie Audit-Eintrag prüfen;
  bestätigen, dass keine Fachnachricht entstand und die nächste echte Nummer
  folgt; PDF-Vordruckreset bestätigen und die erneute PDF-Erzeugung beobachten,
- Hinweistöne für Fernmelder, Si und Stab/FB in jedem vorgesehenen Browser
  ausdrücklich aktivieren, den Testton und nach der stillen ersten Messung
  jeweils genau einen Hinweis bei realer Warteschlangenerhöhung physisch hören
  sowie die sichtbare Rückmeldung bei ausgeschaltetem oder blockiertem Ton
  kontrollieren; automatisierte Wiedergabeaufrufe ersetzen diese Hörprobe
  nicht,
- Schicht mit Mehrfachfunktion planen, annehmen, aktivieren, den eigenen Hut
  wechseln und mit nachvollziehbarer Übergabe ablösen,
- einen S6-Fernmeldeplan versionieren und einen Melderauftrag von der
  Beauftragung bis zur Rückmeldung vollständig durchlaufen,
- ETB-Berichtigung als neuen Eintrag, getrennte Ereignis-/Erfassungszeit,
  Referenzen und chronologische Darstellung prüfen; ETB und TBB mit mehreren
  ausgewählten aktiven Hüten lesen und fachfremde Schreibversuche abweisen,
- Abschluss-Preflight, formalen Einsatzabschluss, einjährige Mindestfrist und
  Aufbewahrungssperre prüfen,
- erzeugten Vordruck drucken und auf einer realen Rückfallebene lesen; das
  PDF-Dossier einmal mit allen neun Abschnitten öffnen und die
  Nachrichten-/Betriebsnachweisketten sowie einen eingebetteten Anhang prüfen,
- vollständiges Backup auf einem getrennten Host wiederherstellen und dort
  Nachricht, Anhang, Vordruck, ETB/TBB sowie Export stichprobenartig öffnen.

Ein Screenshot allein ist kein Nachweis. Für jeden Lauf werden mindestens
Commit, Image-Digests, MariaDB-Version, Browser, Zeitpunkt, Prüfer, verwendete
Funktionen und Ergebnis festgehalten.

## Freigabeprotokoll

```text
Git-Commit:
App-Image-Digest:
Migrate-Image-Digest:
MariaDB-Image-Digest/Version:
Testzeitpunkt und Zeitzone:
Testumgebung/Compose-Projekt:
Automatisierte Suite: PASS/FAIL, Log-/Artefaktpfad:
Browser-Akzeptanz: PASS/FAIL, Browser-Version, Artefaktpfad:
Restore-Roundtrip: PASS/FAIL, Backup-SHA-256:
Abgenommene Rollen/Funktionen:
Fachliche Bedienabnahme: PASS/FAIL, Prüfer:
Bekannte Abweichungen mit Ticket:
Freigabeentscheidung:
```

Eine Freigabe ist nur zulässig, wenn alle automatisierten Gates erfolgreich
sind und jede im Einsatz verwendete fachliche Funktion abgenommen wurde.
